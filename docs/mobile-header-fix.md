# Mobile Header Fix

The public Render deployment exposed a responsive header issue on narrow mobile screens.

Fixes applied:
- Header stacks cleanly below 900px.
- Brand/logo block is centered and constrained.
- Navigation becomes compact wrapped pill links.
- CTA moves below the nav and uses a safe max width.
- Small-screen typography and logo sizing are reduced.

No route or application logic was changed.