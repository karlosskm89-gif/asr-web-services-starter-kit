# Ecosystem Alignment v10

Purpose: lock the Starter Kit back to the ASR ecosystem after live review showed the demo could still drift toward pale theme styling.

Changes:
- ASR alignment CSS now loads after every profile/theme stylesheet.
- Removed duplicate Services nav item.
- Strengthened red/parchment/blue/gold overrides for body, header, sections, cards, buttons, configurator controls and footer.
- Fixed the configurator nav control value for the Condensed option.
- Archived the obsolete `modules/default/asr.js` coaching-style profile under `docs/history/obsolete-default-asr-profile.js.txt`.
- Removed duplicated root-level historical notes already held under `docs/history/`.

Intent: preserve configurator/theme variation without allowing themes to override the main ASR identity layer.
